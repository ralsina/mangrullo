# This configuration file was generated by `ameba --gen-config`
# on 2025-09-08 00:55:13 UTC using Ameba version 1.6.4.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 1
# Run `ameba --only Metrics/CyclomaticComplexity` for details
Metrics/CyclomaticComplexity:
  Description: Disallows methods with a cyclomatic complexity higher than `MaxComplexity`
  MaxComplexity: 15
  Excluded:
  - src/web_server.cr
  - src/update_manager.cr
  Enabled: true
  Severity: Warning

# Ignore redundant begin block in extract_digest_from_manifest method
# The begin block is needed for proper exception handling with rescue blocks
Style/RedundantBegin:
  Enabled: true
  Excluded:
  - src/image_checker.cr

# not_nil! is commonly used in test assertions and is acceptable in that context
# Disabling this rule as exclusions don't work properly
Lint/NotNil:
  Enabled: false

# Property? syntax doesn't work well with Crystal's type system in this context
Naming/QueryBoolMethods:
  Enabled: false

# The formatter has issues with JavaScript regex literals in HTML strings
Lint/Formatting:
  Enabled: true
  Excluded:
  - src/web_views.cr
